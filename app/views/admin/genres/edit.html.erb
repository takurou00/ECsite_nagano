<div class="container">
  <div class="row d-flex justify-content-center">
    <h4 class="h4 my-1">ジャンル編集</h4>
  </div>
  <%= form_with model: [ :admin, @genre ], local: true do |f| %>
    <div class="row d-flex justify-content-center">
      <div class="col-md-5">
    		<table class='table table-borderless mt-5'>
          <tr>
            <th>ジャンル名</th>
            <td>
              <%= f.text_field :name %>
            </td>
            <td>
              <%= f.submit "変更を保存", class: "btn btn-success" %>
            </td>
          </tr>
        </table>
      </div>
    </div>
  <% end %>
</div>